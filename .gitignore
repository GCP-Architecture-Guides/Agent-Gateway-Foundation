# =============================================================================
# Agent Gateway Foundation — .gitignore
# =============================================================================

# -----------------------------------------------------------------------------
# SENSITIVE: Project-specific configuration — NEVER commit these
# Copy terraform.tfvars.example → terraform.tfvars and fill in your values.
# -----------------------------------------------------------------------------
terraform.tfvars
*.tfvars
!*.tfvars.example

# -----------------------------------------------------------------------------
# scripts/deploy_chat_agent.sh IS committed — it reads all values from
# terraform.tfvars at runtime (no hardcoded project IDs or gateway names).
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# GENERATED: agents/chat-agent/.env is written by deploy_chat_agent.sh with
# runtime project credentials (project ID, gateway paths). Never commit it.
# -----------------------------------------------------------------------------
agents/chat-agent/.env

# -----------------------------------------------------------------------------
# GENERATED: Temporary gateway_agent SDK bundle copied into the agent dir
# during deploy and cleaned up afterwards. Leftover only if deploy crashed.
# -----------------------------------------------------------------------------
agents/chat-agent/gateway_agent/

# -----------------------------------------------------------------------------
# Terraform state and local backend files
# -----------------------------------------------------------------------------
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
*.tfstate.*.backup
terraform.tfstate.d/
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# -----------------------------------------------------------------------------
# Python
# -----------------------------------------------------------------------------
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/
.eggs/
*.egg

# -----------------------------------------------------------------------------
# Test results (contain project IDs and response data)
# -----------------------------------------------------------------------------
test-agent/results/

# -----------------------------------------------------------------------------
# Scratch / temp files
# -----------------------------------------------------------------------------
scripts/scratch/
logs/
*.log
deploy_*.log
*.tmp

# -----------------------------------------------------------------------------
# OS / Editor
# -----------------------------------------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
*~

# Agent config — generated by scripts/generate_agent_config.sh (project-specific, never commit)
agent.env
